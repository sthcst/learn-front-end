# Example env file for local development
# Copy this to .env.local and modify values as needed

# Optional dev password used by the demo LoginForm
VITE_DEV_PASSWORD=password

# Optional API base URL (for future integration)
VITE_API_URL=http://localhost:4000
